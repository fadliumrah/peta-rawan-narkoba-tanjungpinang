{
  "name": "peta-rawan-narkoba",
  "version": "1.0.0",
  "dependencies": {
    "cloudinary": "^1.41.3",
    "multer-storage-cloudinary": "^4.0.0"
  },
  "scripts": {
    "build:client": "npm --prefix client ci && npm --prefix client run build",
    "start:prod": "npm --prefix server start:prod",
    "heroku-postbuild": "npm --prefix client ci && npm --prefix client run build",
    "start": "npm --prefix server start"
  }
}
